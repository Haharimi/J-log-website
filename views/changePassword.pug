
extends layouts/main

block content
     .form__container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", name="oldPassword", placeholder="기존 비밀번호")
            input(type="password", name="newPassword", placeholder="신규 비밀번호")
            input(type="password", name="newPassword1", placeholder="신규 비밀번호 확인")
            input(type="submit", value="비밀번호 변경하기") 